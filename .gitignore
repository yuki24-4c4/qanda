# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# Temporary files generated by your text editor or operating system
# belong in git's global ignore instead:
# `$XDG_CONFIG_HOME/git/ignore` or `~/.config/git/ignore`

# Ignore bundler config.
/.bundle

# Ignore all environment files.
/.env*

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore storage (uploaded files in development and any SQLite databases).
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

/public/assets

# Ignore master key for decrypting credentials and more.
/config/master.key

# --- Bundler（vendor に gems を置く場合） ---
/vendor/bundle/

# --- Node / フロントエンド（使っている場合） ---
/node_modules/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
# Webpacker
/public/packs
/public/packs-test
# Vite
/public/vite*
.vite/

# --- tmp のサブディレクトリ（.keep は残す） ---
/tmp/cache/*
!/tmp/cache/.keep
/tmp/sockets/*
!/tmp/sockets/.keep

# --- ActiveStorage は既にOK。念のため tmp 側もカバー済み ---

# --- 資格情報キー（複数環境の key） ---
/config/credentials/*.key

# --- Kamal（デプロイ秘密） ---
.kamal/secrets*
.kamal/*.env
.kamal/tokens/

# --- アップロード系（CarrierWave/Shrine 等を使う場合） ---
/public/system
/public/uploads
/public/attachments

# --- テスト生成物 / カバレッジ ---
/coverage/
/spec/examples.txt
.rspec_status
capybara-*.png
/.byebug_history

# --- SQLite を使っている場合だけ ---
/db/*.sqlite3
/db/*.sqlite3-*
/db/*.sqlite3-journal

# --- 環境ファイルのサンプルは許可 ---
!.env.sample
!.env.example

# --- RuboCop キャッシュ ---
.rubocop-cache
.rubocop-https*
